spring:
      application:
        name: blog
      datasource:
        url: jdbc:postgresql://localhost:5432/blog
        username: postgres
        password: postgres
      jpa:
        hibernate:
          ddl-auto: update
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET}